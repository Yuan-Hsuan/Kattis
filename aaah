#include<bits/stdc++.h>
using namespace std;

int main(){

    ios_base::sync_with_stdio(0);
    cin.tie(0);
    
    string a, b;
    cin >> a >> b;

    if(a.length() < b.length()) {
        cout << "no";
    }
    else {
        cout << "go";
    }
}
